{
    "_comment": "This sample JSON file can be used for running RPiIP file from cloud storage. The arguments passed here in `steps`->`action`->`args` are valid file URL and path where we want to store the downloaded file on the device.",
    "version": "1.0",
    "steps": [
      {
        "action": {
          "name": "Publish IP",
          "type": "runHandler",
          "input": {
            "handler": "publishRPiIP.py",
            "path": "default"
          },
          "runAsUser": "root"
        }
      }
    ]
  }